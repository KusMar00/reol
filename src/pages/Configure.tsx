import NavBar from "../components/NavBar";

const Configure = () => {
  return (
    <div className="h-screen flex flex-col bg-black items-center justify-between">
      <h1 className="text-8xl text-white mt-80">Configure</h1>
      <NavBar />
    </div>
  );
};

export default Configure;
